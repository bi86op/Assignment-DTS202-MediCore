apiVersion: v1
kind: Service
metadata:
  name: medicore-service
spec:
  type: NodePort
  selector:
    app: medicore
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080